{
  "files": [],
  "compilerOptions": {
    "jsx": "react-jsx", // For React 17+ (for older React versions use "react")
    "allowJs": true,     // Allows JavaScript files to be part of the project
    "resolveJsonModule": true, // Ensures module resolution works correctly
    "esModuleInterop": true, // Required for proper imports in many cases
    "moduleResolution": "node"
  },
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
